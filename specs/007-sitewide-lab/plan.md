# Plan

Spec Kit scripts/skills remain absent from this checkout. Equivalent specify,
plan and task artifacts precede implementation; no tool bootstrap is needed.

1. Inventory routed templates, scripts/forms and inherited layout collisions.
2. Inspect Stitch brand assets and production reference model relationships.
3. Take bounded read-only taxonomy/geography snapshot, validate and seed a fresh
   local QA database. Keep original local and production databases untouched.
4. Consolidate shared shell and scoped compatibility styles; refactor page
   layouts where CSS alone cannot preserve readable, responsive controls.
5. Apply correctly sized logo/default banners and built-in translations.
6. Run full regression and broader responsive/auth/form/no-JS evidence, then
   commit and update own ledger row/record/Handoff Log. Deployment remains HOLD.

Payments/idempotency: no endpoint or state-transition change. Browser sends are
local fixtures only; external network is blocked. Rollback is source/image-only;
no production schema/data migration is part of this work.

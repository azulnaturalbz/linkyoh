# Plan

1. Compare the public reference with the hub-owned source without editing it.
2. Apply the nav delta and vendor the published stylesheet bytes. Add Games
   to the existing sister identity and related-products output only.
3. Update narrow regression expectations and add offline contract/parity checks.
4. Run the existing test suite in isolated local PostgreSQL/Redis; exercise
   the existing preview with external traffic blocked, analytics stubbed,
   and EN/ES keyboard/no-JS responsive screenshots.
5. Commit in Linkyoh. Update the canonical owner row/record and append one
   handoff with evidence. Deployment is a separate human gate.

No model or runtime configuration changes are required. Removing the v2
consumer commit restores the v1 presentation; no data rollback is involved.
